spring:
  application:
    name: user-management-service
  r2dbc:
    url: URL-DATA-BASE
    username: USER-DATABASE
    password: PASSWORRD-DATABASE

server:
  port: 8081

# Configuraci�n de logging
logging:
  level:
    root: INFO
    org.springframework: INFO